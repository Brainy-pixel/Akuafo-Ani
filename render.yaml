services:
  - type: web
    name: akuafo-ani
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -c "from stable_twi_tts import StableTwiTTS; StableTwiTTS.from_pretrained(cache_dir='models/', quiet=True)"
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false  # set manually in the Render dashboard — a Postgres
                     # connection string (e.g. from Neon), so accounts
                     # survive redeploys instead of living on local disk
